<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.SurveyResultMapper">


	<insert id="saveResult"
		parameterType="com.example.demo.model.SurveyResult">
		INSERT INTO survey_result (survey_id, q_num, client_id, answer_num,
		answer_text)
		VALUES (#{surveyId}, #{qNum}, #{clientId}, #{answerNum}, #{answerText})
		ON DUPLICATE KEY UPDATE
		answer_num = #{answerNum},
		answer_text = #{answerText}
	</insert>



</mapper>