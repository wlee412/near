<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.SurveyResultMapper">


	<insert id="saveResult" parameterType="SurveyResult">
		INSERT INTO survey_result (survey_id, q_num, client_id, score)
		VALUES (#{surveyId}, #{qNum}, #{clientId}, #{score})
	</insert>


</mapper>